import { OverdueHighlightDirective } from './overdue-highlight.directive';

describe('OverdueHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new OverdueHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
